version: 2

builds:
  - id: "llm-mux"
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./cmd/server/
    binary: llm-mux
    ldflags:
      - -s -w -X 'main.Version={{.Version}}' -X 'main.Commit={{.ShortCommit}}' -X 'main.BuildDate={{.Date}}'

archives:
  - id: "llm-mux"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - LICENSE
      - README.md
      - config.example.yaml

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

homebrew_casks:
  - name: llm-mux
    binaries:
      - llm-mux
    repository:
      owner: nghyane
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://github.com/nghyane/llm-mux"
    description: "Multi-provider LLM gateway with unified API"
    caveats: |
      To use llm-mux, create a config file at ~/.config/llm-mux/config.yaml

      Run with: llm-mux -config ~/.config/llm-mux/config.yaml
